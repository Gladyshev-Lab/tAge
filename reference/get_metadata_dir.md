# Get path to the package metadata directory

Get path to the package metadata directory

## Usage

``` r
get_metadata_dir()
```

## Value

Character string with the full path to the metadata directory.
