% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/genes.R
\name{batch_convert_gene_ids}
\alias{batch_convert_gene_ids}
\title{Convert gene IDs for multiple species in batch}
\usage{
batch_convert_gene_ids(
  gene_ids,
  species,
  from_type = "auto",
  to_type = "entrez",
  verbose = TRUE
)
}
\arguments{
\item{gene_ids}{A list of character vectors, where each element contains gene
identifiers for one species. Names should correspond to species names.}

\item{species}{A character vector of species names corresponding to each element
in gene_ids. Must have the same length as gene_ids.}

\item{from_type}{Character string specifying the input gene ID type. Options: "auto",
"symbol", "ensembl", "refseq", "uniprot", "entrez". Default is "auto".}

\item{to_type}{Character string specifying the output gene ID type. Options: "entrez",
"symbol", "ensembl". Default is "entrez".}

\item{verbose}{Logical indicating whether to print progress messages. Default is TRUE.}
}
\value{
A list with converted gene information for each species (see convert_gene_ids
  for details of individual results).
}
\description{
This function converts gene identifiers for multiple species in a single call.
It processes each species separately and returns a list with results for each species.
}
\examples{
# Convert genes for multiple species
gene_lists <- list(
  mouse = c("Actb", "Gapdh"),
  human = c("ACTB", "GAPDH")
)
species <- c("mouse", "human")
converted <- batch_convert_gene_ids(gene_lists, species)
}
