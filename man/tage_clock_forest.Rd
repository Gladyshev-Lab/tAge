% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/figures.R
\name{tage_clock_forest}
\alias{tage_clock_forest}
\title{Forest plot of clock effects}
\usage{
tage_clock_forest(
  data,
  value_columns = NULL,
  group_column,
  reference_group = NULL,
  compare_groups = NULL,
  clocks_meta = NULL,
  covariates = NULL,
  split_by = NULL,
  se_columns = NULL,
  variance_strata = c("subset", "all_data"),
  p_adjust = "BH",
  p_adjust_scope = "across_columns",
  conf_level = 0.95,
  sig_threshold = 0.05,
  sort_by_effect = TRUE,
  label_column = "name",
  outcome_column = "outcome",
  units = NULL,
  title = "Clock effects",
  subtitle = NA,
  caption = NULL,
  base_size = 11,
  row_height = 0.3,
  panel_width = 4.9,
  width = NULL,
  height = NULL,
  stats = NULL
)
}
\arguments{
\item{data}{Data frame of per-sample predictions, e.g. from
\code{\link{predict_tAge}}.}

\item{value_columns}{Prediction columns to show. May be omitted when
\code{clocks_meta} is given, in which case its \code{filename} column is used.}

\item{group_column}{Column holding the experimental groups.}

\item{reference_group}{Level everything is compared against.}

\item{compare_groups}{Levels compared with the reference. \code{NULL} uses
every other level; more than one is drawn as separate coloured series.}

\item{clocks_meta}{Clock table from \code{\link{list_clocks}}. Supplies the
row labels and the outcome each clock predicts, which sets the panel rows,
the colours and the x-axis units.}

\item{covariates, split_by, se_columns, variance_strata, p_adjust, p_adjust_scope, conf_level}{Passed to \code{\link{tage_compare_groups}}. \code{p_adjust_scope} defaults
to \code{"across_columns"}, i.e. correction across the clocks in one panel,
which is what "filled = significant" implies.}

\item{sig_threshold}{Adjusted p-value below which a marker is filled.}

\item{sort_by_effect}{Order clocks by effect size within each panel.}

\item{label_column, outcome_column}{Columns of \code{clocks_meta} holding the
display label and the outcome.}

\item{units}{Named character vector overriding the x-axis unit per outcome,
e.g. \code{c(Chronological = "years")} for human data.}

\item{title, subtitle, caption}{Figure text. \code{subtitle} defaults to a
description of the markers; pass \code{NULL} to drop it.}

\item{base_size}{Base font size.}

\item{row_height, panel_width}{Inches per clock row and per panel column, used
to size the canvas so that rows stay legible as the clock set grows.}

\item{width, height}{Absolute figure size in inches, overriding
\code{row_height} and \code{panel_width}.}

\item{stats}{Pre-computed \code{\link{tage_compare_groups}} output to plot
instead of recomputing.}
}
\value{
A \code{ggplot} object. The statistics are attached as the
  \code{"tage_stats"} attribute and the intended size as
  \code{"tage_size"}; see \code{\link{tage_save_plot}}.
}
\description{
One row per clock: the effect between two groups, whiskers spanning the
confidence interval, filled when the adjusted p-value clears
\code{sig_threshold} and hollow otherwise. Panels are laid out as outcome
(rows) by stratum (columns). This is the figure to reach for when several
clocks are applied to one comparison — a heatmap hides the uncertainty and a
box plot per clock does not fit on a page.
}
\details{
The statistics come from \code{\link{tage_compare_groups}}, so the figure and
the numbers behind it cannot drift apart.
}
\examples{
\dontrun{
clocks <- list_clocks(type = "EN", tissue = "Multi-Tissue")
tage_clock_forest(results, clocks_meta = clocks,
                  group_column = "Genotype", reference_group = "WT",
                  split_by = "Tissue")
}
}
\seealso{
\code{\link{tage_compare_groups}}, \code{\link{tage_module_heatmap}}
}
