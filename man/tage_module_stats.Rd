% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats.R
\name{tage_module_stats}
\alias{tage_module_stats}
\title{Module-clock effect sizes and p-values}
\usage{
tage_module_stats(
  data,
  module_columns,
  group_column,
  reference_group,
  compare_groups = NULL,
  covariates = NULL,
  split_by = NULL,
  variance_strata = c("all_data", "subset"),
  standardize = TRUE,
  p_adjust = TAGE_P_ADJUST_METHODS,
  p_adjust_scope = c("across_columns", "within_column", "global", "none"),
  conf_level = 0.95
)
}
\arguments{
\item{data}{Data frame with one column per module clock.}

\item{module_columns}{Character vector of module-clock columns.}

\item{group_column}{Column defining the experimental groups.}

\item{reference_group}{Reference level of \code{group_column}.}

\item{compare_groups}{Levels compared against the reference. Default
\code{NULL} uses every other level.}

\item{covariates}{Covariate columns. Default \code{NULL}.}

\item{split_by}{Column to stratify on. Default \code{NULL}.}

\item{variance_strata}{\code{"all_data"} (default, matching the
application) fits the p-value model on every group in the stratum;
\code{"subset"} restricts it to the compared groups.}

\item{standardize}{Whether to report standardised effect sizes. Default
\code{TRUE}.}

\item{p_adjust}{Method passed to \code{\link[stats]{p.adjust}}.}

\item{p_adjust_scope}{Correction family. Default \code{"across_columns"}
corrects across modules within each comparison and stratum, which is the
application's "per group" setting; \code{"global"} matches its "globally"
setting.}

\item{conf_level}{Two-sided confidence level for \code{ci_low} / \code{ci_high}.}
}
\value{
A data frame with \code{module}, \code{split}, \code{group1},
  \code{group2}, \code{n}, \code{estimate}, \code{se}, \code{ci_low},
  \code{ci_high}, \code{statistic}, \code{df}, \code{p_value},
  \code{p_adjusted} and \code{label}.
}
\description{
Per-module statistics for the module-clock heatmaps, following the reference
application's \code{Plot_module_heatmap}. With \code{standardize = TRUE}
(the application's default) the two quantities come from different models:
the p-value is the estimated marginal-mean contrast of the full model fitted
on the stratum, while the effect size is the coefficient of a separate
two-group model fitted on values standardised to unit variance, which makes
modules comparable despite their different native scales. With
\code{standardize = FALSE} the estimate, standard error and p-value all come
from the same contrast.
}
\examples{
\dontrun{
tage_module_stats(
  results,
  module_columns  = grep("^module_", names(results), value = TRUE),
  group_column    = "Genotype",
  reference_group = "WT",
  split_by        = "Tissue"
)
}
}
