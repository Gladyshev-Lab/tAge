% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clocks.R
\name{download_clocks}
\alias{download_clocks}
\title{Download clock models from Zenodo}
\usage{
download_clocks(
  clocks,
  dest_dir = "clocks",
  record = .TAGE_ZENODO_RECORD,
  overwrite = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{clocks}{Either a data frame returned by \code{\link{list_clocks}} (the
\code{filename} column is used) or a character vector of model file names.}

\item{dest_dir}{Directory to save the models into. Created if needed.
Default "clocks".}

\item{record}{Character Zenodo record id. Defaults to the published record.}

\item{overwrite}{Logical. Re-download files that already exist. Default FALSE.}

\item{quiet}{Logical. Suppress progress messages. Default FALSE.}
}
\value{
The \code{clocks} data frame with an added \code{path} column.
}
\description{
Downloads the given clock model files from the Zenodo record and returns the
input augmented with a \code{path} column pointing to the local files.
}
\examples{
\dontrun{
clocks <- list_clocks(type = "EN", outcome = "Mortality",
                      species = "Multispecies", tissue = "Multi-Tissue")
clocks <- download_clocks(clocks, dest_dir = "clocks")
model_paths <- list(
  scaled_diff = clocks$path[clocks$scaling == "Scaled"],
  yugene_diff = clocks$path[clocks$scaling == "YuGene"]
)
}
}
