% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{tage_boxplot}
\alias{tage_boxplot}
\title{Box plot of tAge predictions with pairwise significance annotation}
\usage{
tage_boxplot(
  data,
  x_var,
  y_var,
  subgroup_var = NULL,
  colors = NULL,
  point_size = 2,
  point_alpha = 0.7,
  box_width = 0.5,
  stat_method = "t.test",
  comparisons = NULL,
  p_label = "p.signif",
  p_threshold = NULL,
  min_group_n = 2,
  font_size = 12,
  theme_type = "classic",
  title = NULL,
  xlab = NULL,
  ylab = NULL,
  legend_position = "right",
  y_center = NULL,
  y_min = NULL,
  y_max = NULL,
  facet_scales = "free_y",
  x_order = NULL,
  width = 10,
  height = 6
)
}
\arguments{
\item{data}{Data frame of per-sample values, typically the prediction table
returned by \code{\link{predict_tAge}}.}

\item{x_var}{Column name defining the groups on the x axis.}

\item{y_var}{Column name holding the values to plot, e.g. a \code{*_tAge}
column.}

\item{subgroup_var}{Optional column name used to facet the plot. Default
\code{NULL}.}

\item{colors}{Optional named vector of fill colours, keyed by the levels of
\code{x_var}.}

\item{point_size, point_alpha}{Size and opacity of the jittered points.}

\item{box_width}{Width of the boxes.}

\item{stat_method}{Test used for the pairwise comparisons, passed to
\code{ggpubr::stat_compare_means}. Default \code{"t.test"}.}

\item{comparisons}{List of length-2 character vectors giving the pairs to
test. Default \code{NULL} tests all pairs of \code{x_var} levels.}

\item{p_label}{Label style for the annotations, e.g. \code{"p.signif"} for
stars or \code{"p.format"} for numeric p-values.}

\item{p_threshold}{If supplied, comparisons whose p-value is not below this
threshold are removed from the plot. When faceting, a comparison is kept if
it is significant in at least one facet.}

\item{min_group_n}{Minimum number of non-missing observations required in both
groups for a comparison to be shown. When faceting, every facet must meet it.}

\item{font_size}{Base font size; also scales the annotation text.}

\item{theme_type}{ggplot2 theme to apply, e.g. \code{"classic"} or
\code{"minimal"}.}

\item{title, xlab, ylab}{Plot title and axis labels.}

\item{legend_position}{Legend placement passed to \code{ggplot2::theme}.}

\item{y_center}{If given, the y axis is made symmetric around this value —
useful for relative predictions centred on zero.}

\item{y_min, y_max}{Explicit y-axis limits, overriding the automatic range.}

\item{facet_scales}{Scale behaviour across facets, passed to
\code{ggplot2::facet_wrap}. Default \code{"free_y"}.}

\item{x_order}{Character vector giving the order of the \code{x_var} levels.}

\item{width, height}{Plot size in inches, used to set the inline display size.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws a box plot with jittered points for one prediction column, split by a
grouping variable and optionally faceted by a subgroup. Pairwise comparisons
are annotated with brackets; comparisons involving groups with too few
observations, and — when \code{p_threshold} is set — non-significant ones, are
dropped before plotting so the panel stays readable.
}
\examples{
\dontrun{
results <- predict_tAge(tAge_eset, model_paths, species = "mouse", mode = "EN")
tage_boxplot(
  results,
  x_var = "Genotype",
  y_var = "yugene_diff_EN_tAge",
  subgroup_var = "Tissue",
  x_order = c("WT", "Klotho KO"),
  ylab = "Relative tAge, months"
)
}
}
\seealso{
\code{\link{predict_tAge}} for producing the input table.
}
