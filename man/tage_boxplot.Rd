% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{tage_boxplot}
\alias{tage_boxplot}
\title{Create advanced boxplots for tAge analysis}
\usage{
tage_boxplot(
  data,
  x_var,
  y_var,
  subgroup_var = NULL,
  colors = NULL,
  point_size = 2,
  point_alpha = 0.7,
  box_width = 0.5,
  stat_method = "t.test",
  comparisons = NULL,
  p_label = "p.signif",
  font_size = 12,
  theme_type = "classic",
  title = NULL,
  xlab = NULL,
  ylab = NULL,
  legend_position = "right",
  y_center = NULL,
  y_min = NULL,
  y_max = NULL,
  facet_scales = "free_y",
  x_order = NULL
)
}
\arguments{
\item{data}{A data frame containing the data to plot.}

\item{x_var}{Character string specifying the column name for the x-axis variable
(categorical grouping variable).}

\item{y_var}{Character string specifying the column name for the y-axis variable
(continuous variable to plot).}

\item{subgroup_var}{Character string specifying an optional column name for faceting
(e.g., "Tissue"). Default is NULL.}

\item{colors}{Named character vector for manual color mapping of groups. Names should
correspond to group levels. Default is NULL (automatic colors).}

\item{point_size}{Numeric value for the size of jittered points. Default is 2.}

\item{point_alpha}{Numeric value for the transparency of points (0-1). Default is 0.7.}

\item{box_width}{Numeric value for the width of boxplots. Default is 0.5.}

\item{stat_method}{Character string specifying the statistical test method.
Options: "t.test", "wilcox.test", etc. Default is "t.test".}

\item{comparisons}{List of group comparisons for statistical testing. If NULL,
all pairwise comparisons are performed. Default is NULL.}

\item{p_label}{Character string specifying how to display p-values. Options:
"p.signif" (stars), "p.format" (numeric). Default is "p.signif".}

\item{font_size}{Numeric value for the base font size. Default is 12.}

\item{theme_type}{Character string specifying the ggplot2 theme. Options:
"classic", "bw", "minimal". Default is "classic".}

\item{title}{Character string for the plot title. Default is NULL.}

\item{xlab}{Character string for the x-axis label. Default is NULL (uses x_var).}

\item{ylab}{Character string for the y-axis label. Default is NULL (uses y_var).}

\item{legend_position}{Character string specifying legend position. Options:
"none", "right", "bottom", "top", "left". Default is "right".}

\item{y_center}{Numeric value for a central reference line. Can also be used for
symmetric scaling around this value. Default is NULL.}

\item{y_min}{Numeric value for fixed minimum y-axis limit. Default is NULL.}

\item{y_max}{Numeric value for fixed maximum y-axis limit. Default is NULL.}

\item{facet_scales}{Character string for facet scaling. Options: "fixed", "free",
"free_y", "free_x". Default is "free_y".}

\item{x_order}{Character vector specifying the order of x-axis groups. Default is NULL.}
}
\value{
A ggplot2 object.
}
\description{
This function creates publication-ready boxplots with statistical comparisons,
customizable styling, and faceting options. It's designed for visualizing
transcriptomic age analysis results and other comparative data.
}
\examples{
# Create a simple boxplot
library(ggplot2)
data(mtcars)
p <- tage_boxplot(mtcars, x_var = "cyl", y_var = "mpg", 
                  title = "Miles per Gallon by Cylinders")

# Create a boxplot with custom colors and statistical comparisons
p <- tage_boxplot(mtcars, x_var = "cyl", y_var = "mpg",
                  colors = c("4" = "blue", "6" = "green", "8" = "red"),
                  stat_method = "wilcox.test")
}
