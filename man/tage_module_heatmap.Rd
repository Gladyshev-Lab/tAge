% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/figures.R
\name{tage_module_heatmap}
\alias{tage_module_heatmap}
\title{Heatmap of module-clock effects}
\usage{
tage_module_heatmap(
  data,
  module_columns,
  group_column,
  reference_group,
  compare_groups = NULL,
  covariates = NULL,
  split_by = NULL,
  variance_strata = c("all_data", "subset"),
  standardize = TRUE,
  p_adjust = "BH",
  p_adjust_scope = "across_columns",
  sig_threshold = 0.05,
  modules_version = c("4.6", "5.4", "human"),
  module_functions = NULL,
  color_scale = c("robust", "absolute"),
  robust_pct = 95,
  limit = NULL,
  annotate = TRUE,
  digits = 2,
  title = "Module clocks",
  subtitle = NA,
  caption = NULL,
  base_size = 11,
  cell_height = 0.3,
  cell_width = 1.15,
  width = NULL,
  height = NULL,
  stats = NULL
)
}
\arguments{
\item{data}{Data frame with one column per module clock.}

\item{module_columns}{Module-clock columns.}

\item{group_column}{Column holding the experimental groups.}

\item{reference_group}{Reference level.}

\item{compare_groups}{Levels compared against the reference; each becomes a
facet. \code{NULL} uses every other level.}

\item{covariates, split_by, variance_strata, standardize, p_adjust, p_adjust_scope}{Passed to \code{\link{tage_module_stats}}.}

\item{sig_threshold}{Adjusted p-value below which a cell is starred.}

\item{modules_version}{Module set used for the row labels, one of
\code{"4.6"}, \code{"5.4"} or \code{"human"}.}

\item{module_functions}{Named character vector overriding the bundled
module-to-function annotation.}

\item{color_scale}{\code{"robust"} divides each column by its
\code{robust_pct} percentile of \code{|effect|} so differently scaled
columns share one colour bar; \code{"absolute"} uses the raw effect.}

\item{robust_pct, limit}{Percentile used by the robust scale, and the fill
limit.}

\item{annotate}{Whether to print the effect in each cell.}

\item{digits}{Digits used for the printed effect.}

\item{title, subtitle, caption}{Figure text.}

\item{base_size}{Base font size.}

\item{cell_height, cell_width}{Inches per module row and per column, used to
size the canvas so that row labels stay legible as the module set grows.}

\item{width, height}{Absolute figure size in inches, overriding
\code{cell_height} and \code{cell_width}.}

\item{stats}{Pre-computed \code{\link{tage_module_stats}} output.}
}
\value{
A \code{ggplot} object, with the statistics attached as the
  \code{"tage_stats"} attribute and the intended size as
  \code{"tage_size"}; see \code{\link{tage_save_plot}}.
}
\description{
Rows are co-expression modules, columns are strata (or the comparison when
there is no stratification), and each cell carries the effect with a star
when the adjusted p-value clears \code{sig_threshold}. Modules live on
different native scales, so the fill is the effect divided by a robust scale
of its column while the printed number stays in the original units.
}
\details{
The statistics come from \code{\link{tage_module_stats}}.
}
\examples{
\dontrun{
tage_module_heatmap(results, module_columns = modules,
                    group_column = "Genotype", reference_group = "WT",
                    split_by = "Tissue")
}
}
\seealso{
\code{\link{tage_module_stats}}, \code{\link{tage_clock_forest}}
}
