% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats.R
\name{tage_regress_continuous}
\alias{tage_regress_continuous}
\title{Regress tAge on a continuous predictor}
\usage{
tage_regress_continuous(
  data,
  value_columns,
  predictor,
  covariates = NULL,
  split_by = NULL,
  se_columns = NULL,
  p_adjust = TAGE_P_ADJUST_METHODS,
  p_adjust_scope = c("within_column", "across_columns", "global", "none"),
  conf_level = 0.95
)
}
\arguments{
\item{data}{Data frame of per-sample predictions.}

\item{value_columns}{Character vector of tAge columns.}

\item{predictor}{Numeric column regressed against.}

\item{covariates}{Additional fixed-effect columns. Default \code{NULL}.}

\item{split_by}{Column to stratify on. Default \code{NULL}.}

\item{se_columns}{Per-sample standard-deviation columns for Bayesian ridge
clocks; see \code{\link{tage_compare_groups}}.}

\item{p_adjust}{Method passed to \code{\link[stats]{p.adjust}}.}

\item{p_adjust_scope}{Correction family; see \code{\link{tage_compare_groups}}.
Because there is one test per clock and stratum, \code{"across_columns"}
corrects across clocks within a stratum.}

\item{conf_level}{Two-sided confidence level for \code{ci_low} / \code{ci_high}.}
}
\value{
A data frame with \code{value_column}, \code{split}, \code{term},
  \code{n}, \code{estimate} (slope per unit of \code{predictor}), \code{se},
  \code{ci_low}, \code{ci_high}, \code{statistic}, \code{df},
  \code{p_value}, \code{p_adjusted} and \code{label}.
}
\description{
Slope of predicted tAge against a numeric predictor such as chronological
age, dose or time in culture, matching the reference application's "lm"
mode. Elastic net clocks use \code{\link[stats]{lm}}; Bayesian ridge clocks
use a REML meta-regression weighted by the per-sample prediction standard
deviation and report z-tests.
}
\examples{
\dontrun{
tage_regress_continuous(
  results,
  value_columns = "scaled_diff_EN_tAge",
  predictor     = "age_months",
  split_by      = "Tissue"
)
}
}
